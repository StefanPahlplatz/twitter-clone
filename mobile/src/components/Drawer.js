import React from 'react';
import styled from 'styled-components/native';
import { DrawerItems } from 'react-navigation';

const Root = styled.View`flex: 1;`;

const Drawer = props => <Root />;

export default Drawer;
